# Monster Hotel — Todo & Backlog

Légende : ✅ Fait · 🔧 En cours · ⬜ À faire · 🔍 Nécessite setup Unity (pas de code)

---

## Système Employés

| # | Statut | Feature | Notes |
|---|--------|---------|-------|
| 1 | ✅ | Corriger le licenciement (`FireButton` non trouvé dans `HiredSlot`) | Résolu |
| 2 | ✅ | Corriger NullReferenceException dans `FurniturePlacer.CreateGhost` | Résolu |
| 3 | ✅ | Refactoriser `EmployeeData` → pool de prefabs visuels avec auto-reset | Résolu |
| 4 | ⬜ | Corriger onglet Employés vide dans `EmployeeHiringPanel` | Structure `HiredSlot` prefab + `SetText` à vérifier dans l'Inspector Unity |
| 4b | ⬜ | UI licenciement : tous les salariés sur une seule ligne | `hiredContainer` affiche tous les employés embauchés sur une seule ligne au lieu d'un par ligne — probablement un `LayoutGroup` horizontal au lieu de vertical sur le conteneur (même famille de problème que `RoomShopPanel` réglé précédemment). À vérifier dans l'Inspector du prefab/GO `hiredContainer`. |
| 5 | ⬜ | Activer le menu employé en jeu | `EmployeeBoardTrigger` GO à créer dans la scène |
| 6 | 🔍 | Roue d'interaction employé | Code complet (`EmployeeInteractionWheel.cs`). Nécessite Canvas WorldSpace en enfant du prefab employé + refs boutons assignées dans l'Inspector |
| 12 | ✅ | Rating → efficacité (vitesse, pause, fatigue) | `EmployeeInstance.ApplyRatingScaling()` |
| 10 | ⬜ | Système de formation | Bouton "Former" dans l'onglet Employés, coût = `rating × trainingCostPerRating`, +1 rating, salaire recalculé |
| 11 | ⬜ | Coût de licenciement basé sur le rating | `fireCompensationPerRating` dans `HotelConfig`, appliqué dans `FireEmployee()` |
| 13 | ⬜ | Bouton "Agrandir l'équipe" dans le header du panel | Coût fixe (`teamSlotUpgradeCost`), +1 slot permanent |
| 14 | ⬜ | Ajouter paramètres dans `HotelConfig` | `trainingCostPerRating`, `fireCompensationPerRating`, `teamSlotUpgradeCost` |
| E1 | ⬜ | Un seul réceptionniste (et en général, limiter certains rôles) | Empêcher le recrutement d'un second réceptionniste s'il y en a déjà un. Prévoir d'autres rôles uniques futurs (ex : manager, chef). |
| E2 | ✅ | Verrouillage de tâche entre employés (anti-doublon) | `IsBeingCleaned` (FurnitureInstance), `IsReserved` (DebrisInstance), `IsReservedForService` (EatingSpot). Réservation synchrone au moment du choix de cible, libération en fin de routine. Réception non concernée (pas de cible mise en cache). |
| E3 | ⬜ | Salle de pause employés | Système de pause désactivé (mis en commentaire dans `EmployeeInstance.cs`) — jamais testé, pas de break room en scène. À reconcevoir : créer une `RoomData`/`FacilityRoomInstance` avec `isBreakRoom = true`, tester la transition GoingToBreak→OnBreak (bug déjà identifié et corrigé dans le code commenté), puis réactiver `UpdateNeeds()`, le bloc pause de `UpdateSchedule()`, `CheckResignation()`, `ForceWork()`, `GoOnBreak()`, `GoToBreakRoom()`. |
| E4 | ⬜ | Point de repli pour nettoyeurs sans tâche | Actuellement `CleaningEmployeeAI` reste immobile s'il n'y a rien à nettoyer/ramasser. Ajouter une pièce/zone (ex : `FacilityRoomInstance` ou point dédié) où l'employé se rend en attendant une nouvelle tâche, plutôt que de rester figé sur place. |
| E5 | ⬜ | Les nettoyeurs doivent jeter les déchets dans une poubelle | Actuellement `PickupDebrisRoutine` (`CleaningEmployeeAI.cs`) fait disparaître le débris instantanément sur place (`DebrisInstance.PickUp()`), sans déplacement vers un point de dépôt. Prévoir un meuble/marker "Poubelle" posable dans l'hôtel (façon `ChairSlot`/`ReceptionDesk`) : après ramassage, le nettoyeur doit se rendre à la poubelle la plus proche avant de faire disparaître le débris. |
| E6 | ⬜ | Définir où vont certains salariés (affectation/zone de travail) | Demandé par l'utilisateur. Aujourd'hui chaque IA employé cible le poste/la ressource disponible la plus proche (pas d'affectation fixe) — à concevoir : assigner un salarié donné à un poste/une zone précise (ex : quelle cuisine parmi plusieurs, quel comptoir), plutôt qu'une résolution automatique. Distinct d'E4 (point de repli des nettoyeurs sans tâche, cas spécifique déjà tracké) — recoupement à clarifier avec C8 (plusieurs cuisines) quand ce chantier démarrera, la même question d'assignation s'y pose. |

---

## IA Employés

| # | Statut | Feature | Notes |
|---|--------|---------|-------|
| 7 | ✅ | IA Réceptionniste | Code complet. Nécessite `ReceptionDesk` GO dans la scène |
| 8 | ✅ | IA Nettoyeur | Code complet. Gère meubles sales + débris |
| 9 | ✅ | IA Cuisinier | Code complet. Flux frigo → plan de travail → livraison → retour |

### Détail IA Cuisinier ✅
- Attente au comptoir (WorkbenchStation)
- Sélection de recette par type de monstre (`HotelConfig.recipes` + `compatibleMonsters`)
- Vitesse de cuisson scalée par la note
- Qualité de livraison scalée par la note (`cookDeliveryBonusMin` / `cookDeliveryBonusMax`)
- Pause différée via `BlockBreak` — ne coupe plus un cycle de service en cours

---

## UI & Feedback

| # | Statut | Feature | Notes |
|---|--------|---------|-------|
| U1 | ⬜ | Fix UI (général) | Passe de correction sur l'ensemble des panels et HUD. Inclut `DayEndPanel` (U2) : fonctionnel et testé en jeu, mais mise en page brute (textes empilés sans habillage, étoiles en texte "X / 5" plutôt qu'une vraie icône) — à repolir dans cette passe plutôt que dans l'immédiat. |
| U2 | ✅ | Score journalier + récap étoiles | Nouveau `DayScoreManager` (accroché à `TimeManager.OnNewDay`, déjà existant, et `HotelStatsManager.OnGuestDeparted`, nouveau — voir G1) calcule un score pondéré (or gagné net + satisfaction moyenne + renommée gagnée dans la journée, poids/seuils étoiles tunables — valeurs de départ à équilibrer) et affiche `DayEndPanel` (nouveau, `Assets/Scripts/UI/`) en pausant le jeu (`GameManager.PauseGame()` — 1er usage réel de cet état déjà présent mais jamais déclenché nulle part). Écran partagé (pas scopé à un joueur) : n'importe quel joueur peut le fermer avec son action Submit (`GameManager.Players`). **Setup Unity fait et testé en jeu par l'utilisateur** — Canvas/Panel/TMP Texts construits, `DayScoreManager`/`DayEndPanel` branchés. Deux ajustements faits en le testant : étoiles en texte plutôt que caractères ★/☆ (glyphe absent de la police TMP par défaut, s'affichait en carré vide) ; Canvas Scaler `Screen Match Mode` remonté à 0.5 (était proche de 0/Width, causait une dérive verticale du contenu selon le ratio d'écran). **Étendu ensuite (round 1)** : onglet latéral "Récap"/"Monstres" (`CreateTabBar`, même pattern que `RoomManagementPanel`, généré par code), l'onglet Monstres liste la satisfaction moyenne + nb de départs par type de monstre, générée depuis l'enum `MonsterType`. Plusieurs allers-retours pour la cohérence visuelle des textes générés par code (police/taille/alignement copiés depuis `dayLabel` via `ApplyReferenceStyle`, position/espacement mesurés sur l'écart réel `dayLabel`/`scoreLabel` plutôt que deviné). **Étendu ensuite (round 2)** : 3e onglet "Récap général" (jour actuel, or total, nombre de monstres présents — scan live `FindObjectsByType<MonsterDataReference>()`, même pattern que `RoomManagementPanel.cs`), nouveau `SpawnScheduler.OnMonsterArrived` pour compter les arrivées du jour (`arrivalCountLabel`), onglet Monstres enrichi (présence live + renommée globale par type via `HotelStatsManager.RenownForCategory` + renommée gagnée/perdue du jour séparément, pas juste le delta net). Génération de texte par code factorisée dans `DayEndPanel.BuildLineList()`, réutilisée par Général et Monstres. **Étendu ensuite (round 3)** : passage à un système **5 étoiles** (au lieu de 3) côté journalier (`fiveStarScore`/`fourStarScore` ajoutés aux seuils existants, calcul factorisé dans `DayScoreManager.ComputeStars()`) ; nouveau **score global/étoiles global** avec poids/seuils propres (`globalGoldWeight`/`globalSatisfactionWeight`/`globalRenownWeight` + 5 seuils `globalXStarScore`, séparés du journalier car les échelles sont incomparables — or cumulé vs or gagné dans la journée) ; l'onglet **Récap général** reflète maintenant l'équivalent global/cumulé de chaque stat journalière (score global, étoiles globales, satisfaction moyenne sur tous les départs depuis le début, renommée totale toutes catégories confondues via somme de `RenownForCategory`, clients servis au total, arrivées au total), en plus de jour actuel/or total/monstres présents déjà là. Nouveaux accumulateurs jamais remis à zéro (`_totalArrivalsAllTime`/`_totalDeparturesAllTime`/`_satisfactionSumAllTime`) à côté des accumulateurs journaliers existants qui, eux, sont bien resetés à chaque nouveau jour. Tous les seuils/poids (journalier et global) sont des champs publics sur le composant `DayScoreManager` dans l'Inspector — valeurs de départ à équilibrer par l'utilisateur. Reste en mise en page brute — voir U1 pour le polish visuel. Onglet Général passé à 9 lignes : à surveiller si ça déborde verticalement du panel (même risque que déjà rencontré sur l'onglet Monstres). **Ajusté ensuite** : Général et Monstres alignés sur exactement la même largeur/police que Journalier (plus de réduction) ; Monstres utilise un retour à la ligne (`wordWrap`, hauteur mesurée via `GetPreferredValues`) plutôt qu'une police réduite quand une ligne dépasse. **Étendu ensuite (round 4)** : nouveau `GuestChannel` (`Room`/`Restaurant`) propagé depuis `SpawnScheduler.OnMonsterArrived` jusqu'à `HotelStatsManager.ReportGuestDeparture` (+ nouveau paramètre `served`, distingue un client resto effectivement nourri d'un client resto reparti sans avoir été servi — abandon/délai dépassé, alors que côté chambre `served` est toujours vrai). Nouveau `HotelStatsManager.ReportMealRevenue()`/`OnMealRevenue`, appelé depuis `EatingSpot.PayForMeal()`, pour le gain resto par type de monstre. Arrivées/clients servis désormais comptés séparément par canal, journalier ET cumulé. Nouveau conteneur généré par code `DayEndPanel.recapExtraContent` (enfant de `RecapContent`) pour afficher ce détail sans texte manuel supplémentaire. **Étendu ensuite (round 5, mise en page automatique)** : `_linePitch` calculé depuis la police (`dayLabel.fontSize × linePitchMultiplier`, nouveau champ réglable) au lieu d'être mesuré sur l'écart `dayLabel`/`scoreLabel` (trop généreux, cascadait dans tous les onglets → panel "trop haut"). Les 7 textes manuels du Récap journalier (`scoreLabel` à `renownLabel`) sont désactivés et remplacés par une liste générée unique dans `recapExtraContent`, seule `dayLabel` reste manuelle. `dismissPromptLabel` est repositionné automatiquement juste sous le contenu réel de l'onglet actif à chaque changement d'onglet (`BuildLineList` retourne le Y de fin de liste). Setup Unity inchangé — tout ce round est côté code. **Étendu ensuite (round 6, migration UI Toolkit)** : le correctif du round 5 n'a pas suffi (toujours trop haut, wrap toujours mal géré par `GetPreferredValues` sur un `TextMeshProUGUI` fraîchement créé). `DayEndPanel.cs` entièrement réécrit sur `UIDocument`/`VisualElement` (Flexbox) au lieu d'uGUI — layout automatique natif (wrap, empilement, position du bouton Submit), plus aucun calcul de position à la main. `DayScoreManager.cs` inchangé (signature `Show()` identique). **Setup Unity confirmé et testé par l'utilisateur** : `UIDocument` ajouté sur `DayEndCanvas` (même GameObject que le script `DayEndPanel`), ancienne hiérarchie `DayEndRoot` laissée en place mais inerte (plus référencée). 3 bugs trouvés et corrigés en itérant avec l'utilisateur (voir CHANGELOG pour le détail) : ordre `Awake`/`OnEnable` entre composants (fix : construction déplacée dans `Start()`), hauteur à 0 avec `Position.Absolute` (fix : `width/height: 100%`), et surtout **`CreateRuntimePanelSettings()` (création automatique du PanelSettings) a un bug de dimensionnement non résolu** — la voie fonctionnelle et recommandée est d'assigner un asset PanelSettings existant dans l'Inspector (`FeelUIToolkitDemoPanelSettings.asset` du plugin Feel, réutilisé tel quel). Panel confirmé fonctionnel en jeu. **Étendu ensuite (round 7, effets Feel/MMFeedbacks)** : structure passée d'une génération 100% par code à un fichier UXML (`Assets/Scripts/UI/DayEndPanel.uxml`, écrit à la main, structure/noms uniquement — le style reste appliqué par code depuis les champs Inspector) — nécessaire pour que les feedbacks Feel retrouvent leurs éléments cibles de façon fiable dès le démarrage de la scène (un élément généré par code n'existe pas encore quand `MMF_Player` tente sa recherche initiale, peu importe l'ordre des composants ; un UXML est cloné par `UIDocument` avant tout `Start()` de la scène). Cause finale du "le feedback ne fait rien" : `Timescale Mode` du feedback sur `Scaled` alors que `DayScoreManager` met le jeu en pause juste après avoir affiché le panel — l'animation, pilotée par du temps *scaled*, se figeait quasi instantanément à sa valeur de départ (panel à l'échelle ~0, donc invisible). Fix Inspector uniquement : `Timescale Mode` = `Unscaled`. Nouveau champ `DayEndPanel.tabBounceFeedbacks[]` (3 `MMF_Player`, un par onglet) : `SwitchTab()` déclenche explicitement celui du bon index, Feel ne pouvant pas savoir tout seul quel bouton a été cliqué. **Setup Unity requis** : glisser `DayEndPanel.uxml` dans `Source Asset` du `UI Document` ; un `MMF_Player` par élément à animer (`Panel`, `Tab_0`/`1`/`2`, etc.), chacun avec `Timescale Mode` = `Unscaled` impérativement. Confirmé fonctionnel en jeu par l'utilisateur (rebond sur "Panel"). **Étendu ensuite (round 8, feedbacks par ligne + espacements séparés)** : `RebuildLines()` nomme désormais chaque ligne générée (`{conteneur}_Line_{index}`, ex. `Content_1_Line_0`) et leur donne une classe USS commune `stat-line` — permet à Feel de cibler une ligne précise par nom OU toutes les lignes d'un coup via `Query Mode = Class`. Piège trouvé : les lignes de stats sont détruites/recréées chaque jour (contrairement à `Panel`/`Tab_X`), donc une recherche Feel mise en cache une seule fois au démarrage devient obsolète dès le 2e jour — fix : `Initialization()` est maintenant rappelé juste avant `PlayFeedbacks()` à chaque fois (dans `Show()` pour `showFeedback`, dans `SwitchTab()` pour `tabBounceFeedbacks`), pas seulement une fois au lancement. `lineSpacing` (espacement entre lignes de stats) et l'espacement des boutons d'onglet partageaient le même champ — séparés en deux : `lineSpacing` (texte uniquement) et nouveau `tabButtonSpacing` (boutons uniquement). Nouveau champ `paragraphSpacing` (`style.unityParagraphSpacing`) pour l'interligne à l'intérieur d'une ligne qui retourne (surtout utile sur l'onglet Monstres, lignes denses) — distinct de `lineSpacing` qui espace des lignes séparées. **Référence complète des noms/classes ciblables par Feel** : voir section dédiée plus bas dans ce fichier ("Récap fin de journée — éléments ciblables par Feel"). **Étendu ensuite (round 9, config score + Confort/attractivité chambres)** : poids/seuils de score sortis de `DayScoreManager` vers un nouveau `ScoreConfig` (`HotelConfig.Score`, même pattern que `EconomyConfig`) — **setup requis : créer l'asset via `Create > Hotel > Config > Score` dans `Assets/Resources/Config/`, nommé exactement `ScoreConfig`**, sinon `DayScoreManager` plante au premier jour. Nouveau `HotelStatsManager.RecalcRoomAttractiveness()` : l'attractivité des chambres (base + bonus meubles) alimente désormais la renommée de l'hôtel (effet systémique — légendaires, vitesse de spawn — pas juste le score), recalculée une fois par jour. Confort de l'hôtel ajouté au score global (nouveau `globalComfortWeight`, pas de version journalière — le Confort ne se "gagne" pas au jour le jour). Nouvelle ligne "Confort total" dans Récap général. |
| U3 | ✅ | Nettoyage de la table après repas | Débris spawned après repas via `EatingSpot.SpawnMealDebris()`. Slot bloqué tant que `IsDirty`. Nettoyeur ramasse via `DebrisInstance.All`. Setup Unity : assigner `mealDebrisPrefabs[]` sur chaque `EatingSpot`. |
| U4 | ⬜ | Menu pause | `GameManager` a déjà l'état `GameState.Paused` + `PauseGame()`/`ResumeGame()` (coupe `Time.timeScale`), mais **aucune UI ni aucun input ne les déclenche** — à créer entièrement : écran pause (reprendre/options/quitter), touche/bouton pour l'ouvrir, gérer le multijoueur (qui peut pauser ? les deux joueurs sont-ils affectés ?). |
| U5 | ✅ | Suggestion auto de chaises à la construction d'une table | Nouveau champ `FurnitureData.matchingChair` : après la pose d'une table qui le renseigne, enchaîne directement en mode placement de la chaise assortie (sans repasser par le menu). **À faire manuellement dans l'éditeur : assigner `Matching Chair` sur les assets de table existants**, sinon le champ reste vide et rien ne change. |
| U6 | ⬜ | Indicateur plus visible : pièce libre / à nettoyer / à réparer | Au départ d'un monstre, si la pièce/le mobilier reste sale ou abimé (`FurnitureInstance.IsDirty`/`IsDamaged`), l'indication actuelle est peu visible pour le joueur — prévoir un signal plus clair (icône au-dessus de la pièce, surbrillance, prompt...) pour que le joueur sache qu'il faut nettoyer/réparer avant le prochain client. **Étendu par l'utilisateur** : aussi indiquer si une chambre est simplement libre/disponible (pas seulement sale/abimée) — un même système d'icône/surbrillance par état (`RoomState.Empty` propre vs sale vs abimée) couvrirait les deux besoins. |
| U7 | ⬜ | Tutoriel | Demandé par l'utilisateur. Pas de système d'onboarding actuellement — à concevoir (portée, étapes, déclenchement). |
| U8 | ⬜ | Menu principal | Demandé par l'utilisateur. Distinct du menu pause (U4, déjà noté) — écran de démarrage/titre, pas trouvé dans le projet actuellement. |

---

## Gameplay & Monstres

| # | Statut | Feature | Notes |
|---|--------|---------|-------|
| G1 | ✅ | Satisfaction → Renommée → monstres légendaires | Nouveau `HotelStatsManager.RenownForCategory(MonsterType)` = base décorations (partagée) + renommée "gagnée" par catégorie (`Dictionary<MonsterType,float>`), alimentée par `ReportGuestDeparture(type, satisfaction, angry)` — appelé à chaque départ réel : `ReservationSystem.CheckoutNow()`/`CheckoutEarly()` (client chambre) et `MonsterNeedSeeker.TryStayOrExit()`/`ExitDissatisfied()` (visiteur repas, y compris timeout jamais servi — routé maintenant via `ExitDissatisfied` au lieu d'`ExitHotel()` direct, comble le trou explicitement pointé par cette note : `GiveUpMeal`/`giveUpMealPenalty` avait effectivement zéro impact hors du monstre lui-même avant ce chantier). `SpawnScheduler.PassesLegendaryGate()` compare désormais à `RenownForCategory(data.monsterType)` au lieu de `TotalRenown` global. Nouveaux tunables sur `HotelStatsManager` (`satisfactionRenownThreshold`, `renownPerSatisfactionPoint`, `angryDepartureRenownPenalty`) — valeurs de départ, à équilibrer. Aucun champ ScriptableObject touché (MonoBehaviour de scène). |
| G2 | ⬜ | Intrus humain | Un humain entre aléatoirement dans l'hôtel. Le joueur doit l'intercepter avant qu'il ressorte. S'il repart, la réputation de l'hôtel (secret) est impactée négativement. Possible lien avec G8 (fantôme) — l'humain intrus pourrait être l'hôte que le fantôme cherche à posséder. |
| G8 | ⬜ | Nouveau monstre : Fantôme (possession) | Idée utilisateur 2026-08-05 : le fantôme ne peut rien faire (pas de besoins traités, pas assignable en chambre) tant qu'il n'a pas possédé un humain — l'humain possédé fait alors office de "chambre" pour le fantôme (remplace la case chambre habituelle). Questions ouvertes à trancher quand ce chantier démarrera : l'humain à posséder est-il celui de G2 (intrus humain) ou un spawn dédié aux fantômes ? Le joueur doit-il aider à la possession (l'amener au fantôme, l'attraper) ou ça se fait automatiquement si un humain est présent ? Que devient visuellement l'humain une fois possédé (reste humain, ou prend l'apparence du fantôme) ? Qu'est-ce que le fantôme fait s'il n'y a jamais d'humain disponible — reste bloqué indéfiniment au spawn ? |
| G3 | ✅ | Comportement jour/nuit des monstres — restent en chambre selon le moment | Nouveau champ dédié `MonsterData.sleepTime` (`SpawnTime.Any/DayOnly/NightOnly`) — distinct de `preferredSpawnTime` (heure d'arrivée, jamais touché) car sémantique différente (ex: zombie confiné `NightOnly` alors qu'il peut très bien arriver `Any`). Nouveau `MonsterRoamBehavior.IsForbiddenTime` (confiné quand `TimeManager.CurrentSpawnTime == sleepTime`), volontairement distinct d'`IsBlocked` : empêche de sortir en chambre, force un retour immédiat si le sommeil commence en pleine balade (`break`) — mais ne bloque jamais le trajet retour lui-même. **⚠️ Nouveau champ ScriptableObject** (voir précaution habituelle si erreur "script layout incompatible"). Tous les monstres à `Any` par défaut — comportement inchangé tant que non rempli. |
| G4 | ⬜ | Comportements spécifiques par type de monstre | Chaque type de monstre peut avoir un comportement unique. Ex : Zombie → effet "horde" (plus il y en a déjà dans l'hôtel, plus le taux de spawn de zombies augmente). À concevoir comme un système extensible sur `MonsterData` (comportements pluggables plutôt que du code en dur par monstre). |
| G5 | ✅ | Déchets aléatoires en balade | Nouveau `MonsterData.roamLitterChance` (défaut 0.1, **à ajuster**) + `MonsterData.litterPrefabs[]` (vide par défaut = aucun détritus tant que non rempli). Tirage à chaque étape de balade (`MonsterRoamBehavior.TrySpawnLitter()`), même pattern que `EatingSpot.SpawnMealDebris` (détritus non rattaché à une chambre, ramassable par joueur/nettoyeur via `DebrisInstance.All`). **Setup Unity requis : remplir `litterPrefabs[]` sur chaque `MonsterData`**, sinon aucun effet. |
| G6 | 🔧 | Visite sans chambre (restaurant/activités) | Découpé en 5 phases (voir CHANGELOG 2026-07-16 pour le détail Phase 1). **But** : évite que le joueur soit bloqué quand toutes les chambres sont occupées. |
| G6-P1 | ✅ | Phase 1 — Noyau | Tirage aléatoire à chaque spawn (`HotelConfig.mealVisitorChance`). Un visiteur repas ne rejoint pas la file de réception, va directement chercher à satisfaire son besoin (réutilise `FacilityRoomInstance`/`MonsterNeedSeeker` existant), repart une fois servi (`ExitHotel()`). Garde-fou `mealVisitorMaxDuration`. Limite connue : pas de cap dédié pour les visiteurs (`SpawnScheduler.maxPending` ne les compte pas). |
| G6-P2 | ✅ | Phase 2 — Identification visuelle | `MealVisitorBadge` — bulle orange "Repas" world-space au-dessus du visiteur, câblée dans `SpawnScheduler`. |
| G6-B1 | 🔁 | ~~Bug — monstres empilés devant la cuisine~~ | Fix par slots indexés, puis **remplacé** par G6-B6 (réception dédiée) — l'ancienne file ad-hoc devant la cuisine n'existe plus du tout. |
| G6-B2 | ✅ | Bug — bulle "Repas" centrée sur le monstre | Pas un symptôme du chevauchement (infirmé) : `offset` (1.8) ne dépassait pas la tête. Remonté à 2.6. Voir CHANGELOG 2026-07-20. |
| G6-B3 | 🔁 | ~~Jauge d'attente devant la cuisine~~ | Ajoutée à `NoSeatIndicator`, puis **remplacé** par G6-B6 — `NoSeatIndicator` supprimé (plus utilisé), la jauge d'attente reste à faire pour la nouvelle file de réception resto (cf. limite connue G6-B6). |
| G6-B4 | 🔁 | ~~File d'attente cuisine — réajustement auto si obstruction~~ | **Remplacé** par G6-B6 — n'a plus lieu d'être, il n'y a plus de file physique devant la cuisine. |
| G6-B5 | 🔁 | ~~File d'attente cuisine — doit s'adapter dynamiquement~~ | **Remplacé** par G6-B6 — la réception dédiée (comptoir + slots comme l'hôtel) résout le problème à la racine plutôt que de faire suivre un chemin NavMesh à une file devant une porte. |
| G6-B6 | ✅ | Réception du restaurant (remplace B1/B3/B4/B5) | Nouveau `RestaurantReservationSystem` + `RestaurantReceptionDesk` (comptoir posable comme meuble dans la cuisine), même pattern que la réception hôtel (file à slots, validation synchrone de place). Entièrement validé depuis via B7-B22 (comptoir posé, bugs de blocage/crash/traversée de mur corrigés, refonte alternance confirmée en jeu) + la refonte récente de la priorité réceptionniste (par temps d'attente) et le fix StandPoint. Seule limite connue restante : pas de bulle visuelle pour la file resto — voir G6-B23. |
| G6-B23 | ⬜ | Bulle visuelle au-dessus des monstres en file resto | Contrairement à la file hôtel ou aux visiteurs repas (`MealVisitorBadge`), rien n'indique visuellement qu'un monstre attend en file au comptoir resto (vérifié : aucune référence à `GuestBubble` dans `RestaurantReservationSystem.cs`). Ajouter une bulle world-space (même pattern que `GuestBubble`/`MealVisitorBadge`) à l'arrivée en file, masquée au check-in. |
| G6-B24 | ✅ | Réception joueur bloquée par le 1er de la file même si un autre monstre serait acceptable | Nouveau `NextServiceableUnchecked` (`ReservationSystem`/`RestaurantReservationSystem`) — comme `NextUnchecked` mais saute les non-servables, sans filtrer `IsClaimed` (contrairement à la version employé `NextServiceableUnclaimed` : le joueur doit garder la possibilité d'accueillir un monstre déjà réclamé par un employé en chemin). `ReceptionInteractor`/`RestaurantReceptionInteractor` l'utilisent maintenant et repassent le guest explicitement à `CheckInNext(guest)`. |
| G6-B7 | ✅ | Playtest réception resto — check-in avant arrivée + comptoir "ShopCounter" | (1) Check-in validé avant l'arrivée physique du réceptionniste — fixé, attend l'arrivée réelle. (2)(3) Confirmé par les logs : `RestaurantReceptionDesk` avait été ajouté sur le `ShopCounter` existant au lieu d'un meuble séparé dans la cuisine — utilisateur a corrigé côté scène. Voir CHANGELOG 2026-07-20 (suite 3). |
| G6-B8 | ✅ | Monstre bloqué si comptoir resto pas encore construit au moment où il cherche à manger | `RegisterArrival()` abandonnait silencieusement (`HasArrived=true`, aucun déplacement, aucun retry) si `RestaurantReceptionDesk.All` était vide — le monstre restait figé pour toujours, même une fois le comptoir construit ensuite (`_seekingNeed` ne se relançait jamais). Expliquait aussi l'impression que "valider le premier débloque les suivants" (coïncidence : les suivants avaient juste commencé à chercher après coup). Fix : `RegisterArrival()` retourne `bool`, `MonsterNeedSeeker` ne verrouille `_seekingNeed` qu'en cas de succès, sinon retente au prochain cycle. Voir CHANGELOG 2026-07-21. |
| G6-B9 | ✅ | Check-in hôtel impossible pour le joueur | Cause trouvée et confirmée : `ReceptionDesk` s'enregistrait via `Awake()` — absent de tout objet actif de la scène (jamais posé, ou objet resté inactif). Passé à `OnEnable()` + log de confirmation ; utilisateur a ajouté le composant manquant. Voir CHANGELOG 2026-07-21. |
| G6-B10 | ✅ | Rework complet réception (spec utilisateur) | Recap utilisateur : 2 comptoirs (hôtel fixe / resto posé par joueur), 3 types d'arrivée (manger seul / chambre+manger / manger puis chambre), réceptionniste doit **alterner** hôtel/resto (pas de priorité fixe), plusieurs réceptionnistes **sans doublon**, client chambre qui abandonne un repas → satisfaction diminue mais garde sa résa. Implémenté et confirmé en jeu (alternance, chambre nettoyée réassignable). Voir CHANGELOG 2026-07-21 (suite à suite 5). |
| G6-B18 | 🔍 | Deux réceptionnistes convergeaient sur le même poste | `IsClaimed` empêchait de cibler le même monstre, mais pas de se retrouver au même comptoir physiquement. Fix : poste assigné par employé (`_currentPost`), coordonné via registre statique — un poste vide n'est aidé que si personne d'autre ne le couvre déjà. **⚠️ Pas encore testé en jeu avec 2 réceptionnistes.** Voir CHANGELOG 2026-07-22. |
| G6-B19 | ✅ | Conversion visiteur→client et pénalité satisfaction jamais vérifiées en jeu | Confirmé en jeu le 2026-07-27 : les deux fonctionnent (`stayAfterMealChance` testé à 1 temporairement sur un `MonsterNeedSeeker` en Play pour forcer le cas, `giveUpMealPenalty` confirmé via le log `[Cuisine] ... pas servi à temps, retourne en chambre déçu`). |
| G6-B11 | ✅ | Table/EatingSpot bloquée définitivement si le monstre disparaît en attendant | `Update()` retournait indéfiniment sans `Release()` si `_monster` devenait null pendant `WaitingForFood` (départ forcé ailleurs) — `IsOccupied` restait vrai à vie, expliquant "les tables ne se libèrent jamais mais les neuves marchent". Fix : détection + `Release()` forcé. Voir CHANGELOG 2026-07-21 (suite 5). |
| G6-B12 | ✅ | MissingReferenceException en boucle — réceptionniste n'accepte plus personne | `NextServiceableUnclaimed` (resto) accédait à `guest.Monster.transform` sans vérifier `!= null` — un visiteur détruit par son propre timeout de sécurité (90s, indépendant de la file resto) tout en étant encore dans `_pending` provoquait un crash à chaque frame dans `TryStartTask()`, coupant toute la logique de tâche avant même de démarrer. Fix : `Update()` des deux systèmes (hôtel + resto) purge les références mortes, vérifications `!= null` ajoutées partout où le monstre est déréférencé. Explique probablement une bonne partie de l'alternance qui semblait erratique. Voir CHANGELOG 2026-07-21 (suite 6). |
| G6-B13 | 🔍 | Employé bloqué en chemin vers le comptoir resto + validait quand même "à distance" | Deux problèmes : (1) le déplacement vers le comptoir resto semble échouer/bloquer — cause pas identifiée, nécessite investigation séparée (NavMesh ? obstruction ?) ; (2) ✅ corrigé — `CheckInNext()` était appelée après le timeout de 15s même sans arrivée confirmée, validant le monstre "à distance". Ne valide plus que si l'arrivée est confirmée, sinon `LogWarning` explicite. **À tester** : le nouveau warning "n'a jamais atteint le comptoir... bloqué en chemin ?" doit maintenant apparaître systématiquement quand (1) se reproduit — utile pour la suite du diagnostic. Voir CHANGELOG 2026-07-21 (suite 7). |
| G6-B14 | ✅ | Prefabs Zombie/Vampire/Werewolf — "script manquant" | `NoSeatIndicator` avait été figé en dur sur `Zombie.prefab` (racine) et séparément sur `Vampire.prefab` (`Werewolf.prefab` en héritait via variante) — cassé par la suppression du script ce chantier. Fix : retiré proprement des deux prefabs (édition YAML directe), vérifié qu'aucune trace ne subsiste ailleurs dans le projet. **Ma faute** — leçon retenue : vérifier les `.prefab` avant de supprimer un `.cs`, pas seulement les références C#. Voir CHANGELOG 2026-07-21 (suite 9). |
| G6-B15 | ✅ | Régressions de mouvement (client hôtel, réceptionniste, nettoyeur) signalées en même temps que B14 | Résolu par le fix des prefabs (B14) — confirmé en jeu, "ça a l'air de fonctionner". Voir CHANGELOG 2026-07-21 (suite 9). |
| G6-B16 | ✅ | Réceptionniste — trajet hôtel↔resto pas systématique | Cause trouvée : `MonsterMover.NavigateTo()` traitait `NavMeshPathStatus.PathPartial` comme `PathComplete` — le personnage suivait le chemin jusqu'au dernier point atteignable (zones pas complètement reliées sur le NavMesh) puis s'arrêtait, sans jamais atteindre la vraie cible. Fix défensif : complète le trajet en ligne droite si le chemin était partiel. **Vérifier aussi le NavMesh lui-même** (Window → AI → Navigation) entre les deux comptoirs pour un trajet propre plutôt qu'une marche en ligne droite pouvant traverser les murs. Voir CHANGELOG 2026-07-21 (suite 11). **Suite 2026-07-27 (G6-B20)** : le filet ligne droite lui-même traversait les murs sans limite de distance — plafonné. |
| G6-B21 | ✅ | Cuisinier traversait les murs + apparaissait enfoncé dans le sol | Deux causes cumulées, toutes deux confirmées en jeu le 2026-07-27 : (1) `EmployeeData_Cook.asset` → `characterPrefabs[0]` référençait un prefab supprimé/déplacé (GUID introuvable) → repli capsule sans `NavMeshAgent` + pivot centré. Réassigné par l'utilisateur, + repli capsule blindé côté code (warning explicite + `NavMeshAgent` si jamais ça se reproduit sur un autre rôle). (2) `EmployeeManager.Hire()` ajoutait le **Y complet** du prefab par-dessus le spawnPoint (régression du fix position/rotation de la même session) — limité à X/Z seulement, le Y vient toujours du spawnPoint. Les deux corrigés, plus enfoncé dans le sol. Voir aussi G6-B22 pour le "traverse encore les murs vers le resto" qui restait après ça (cause différente, navmesh incrémental). |
| G6-B22 | 🔁 | ~~Rebake NavMesh incrémental laissait des zones déconnectées~~ | Tenté : passer `RoomPlacer`/`ExpansionBlock` de `UpdateNavMesh()` (incrémental) à `BuildNavMesh()` (reconstruction complète) pour éliminer les zones mal connectées. **Reverté** — `BuildNavMesh()` reconstruit tout le NavMesh de l'hôtel à chaque pose de pièce (coût qui grossit avec la taille de l'hôtel), causant un gros ralentissement en jeu à chaque construction. Retour à `UpdateNavMesh()`. Le vrai filet de sécurité reste G6-B20 (warning `[NavMesh]` au lieu de traverser les murs en silence) — si le warning apparaît après une construction, un Bake manuel ponctuel (Inspector → NavMeshSurface → Bake) reste le recours, pas une nécessité systématique. |
| G6-B20 | ✅ | Monstres traversant les murs | **3 causes différentes, toutes trouvées et corrigées lors du playtest du 2026-07-27** : (1) `MonsterMover.NavigateTo()` — filet `PathPartial` (ajouté pour G6-B16) complétait en ligne droite **sans limite de distance** → plafonné par `partialPathMaxSkip` (défaut 2.5m, **à ajuster**) + `LogWarning` au-delà. (2) Même souci sur la branche `PathInvalid` (aucun chemin du tout), pas couverte par le fix (1) → même plafond appliqué. (3) **Cause réelle du cas cuisinier→resto** : le NavMesh bakait carrément à travers un mur trop fin pour la taille de voxel par défaut (~6.7cm, calculée depuis `agentRadius=0.2`) — un rebake complet seul ne suffisait pas (problème de résolution, pas de fraîcheur du bake). **Fix Unity (fait) : `NavMeshSurface` → `Override Voxel Size` coché → `0.02` → Bake.** Confirmé en jeu par capture d'écran du gizmo NavMesh (mur bien bloquant des deux côtés). **Compromis (1)/(2)** : des personnages peuvent apparaître "bloqués" avec un warning `[NavMesh]` en cas de vraie zone déconnectée au lieu de traverser silencieusement — voulu, rend le problème diagnosticable. |
| G6-B17 | ✅ | Aucun revenu pour un repas servi au restaurant | Confirmé : aucune logique de paiement n'existait pour le restaurant, seul le séjour en chambre rapportait. Nouveau `MonsterData.mealRevenue` (défaut 15, **à ajuster par l'utilisateur**) + `EatingSpot.PayForMeal()` appelée à la livraison réussie (avec ou sans plat). S'applique aux clients chambre et visiteurs. **⚠️ Nouveau champ ScriptableObject — fermer/rouvrir l'Éditeur si erreur de build "script class layout incompatible".** Voir CHANGELOG 2026-07-21 (suite 10). |
| G6-P3 | ❌ | ~~Phase 3 — Heures précises~~ | Abandonnée (décision utilisateur 2026-07-22). |
| G6-P4 | ✅ | Phase 4 — Conversion en client chambre | Activée : `MonsterNeedSeeker.stayAfterMealChance = 0.25` (départ, à ajuster), retire `MealVisitorBadge` à la conversion. Limite connue (cas rare, non traité) : monstre multi-besoins pourrait redemander une facilité pendant qu'il attend encore en file hôtel après conversion — voir CHANGELOG 2026-07-22 (suite). Testé et confirmé en jeu le 2026-07-27 (voir G6-B19). |
| G6-P5 | ⏸️ | Phase 5 — Enchaînement d'activités + système de plats | **En pause** (décision utilisateur 2026-07-22). Le monstre peut enchaîner plusieurs activités (nécessite d'autres facilités que la cuisine, à concevoir). Système de plats à score/complexité (remplace le `RecipeData` actuel, très simple). Impact réputation basé sur la qualité du repas. Probablement une feature à part entière plutôt qu'une sous-tâche. |
| G7 | 🔁 | ~~Combats entre monstres à personnalité incompatible~~ | **Doublon, remplacé par S2** (implémenté : `MonsterFightBehavior.cs`, voir section "Interactions sociales monstres"). |
| G9 | ✅ | Spawn client chambre et spawn visiteur repas indépendants par monstre (pas un ratio) | Refonte suite à un premier essai (ratio `mealVisitorChance`) rejeté par l'utilisateur : il veut deux **flux de spawn séparés** qui tournent en parallèle, pas une chance qui se partage entre les deux. `SpawnScheduler` a maintenant 2 timers indépendants par monstre (`_timers`/`_visitorTimers`), chacun avec son propre intervalle : `MonsterData.spawnInterval` (client chambre, existant) et le nouveau `MonsterData.mealVisitorSpawnInterval` (0 = utilise `HotelConfig.Spawn.defaultMealVisitorSpawnInterval`, nouveau champ, 30s par défaut). Un même monstre peut donc arriver comme client chambre ET, séparément, comme visiteur repas. La conversion visiteur→client après repas (G6-P4, `stayAfterMealChance`) reste inchangée. `TrySpawn()` éclaté en `TrySpawnRoomGuest()`/`TrySpawnMealVisitor()` + `SpawnMonsterObject()` commun. **⚠️ Champs ScriptableObject ajoutés/retirés sur `MonsterData` et `SpawnConfig`** — fermer/rouvrir l'Éditeur si erreur "script class layout incompatible" (même précaution que G6-B17/R2). Intervalle visiteur par défaut (30s) à ajuster. |
| G10 | ⬜ | Nouveau monstre : Sorcière | Demandé par l'utilisateur, pas encore détaillé (besoins, comportement, compatibilités sociales/incompatibilités, recettes...) — à concevoir quand ce chantier démarrera, même processus que les monstres existants (`MonsterData`, prefab, controller, needs). |
| G6-B25 | ⬜ | Visiteur repas non servi — retourne à la réception resto au lieu de quitter l'hôtel (bug signalé) | Signalé par l'utilisateur : en jeu, un visiteur repas qui abandonne (jamais servi) retourne systématiquement à la réception resto au lieu de quitter l'hôtel. **Contradiction avec la lecture du code à corriger avant d'agir** : `MonsterNeedSeeker.GiveUpMeal()`/`ExitDissatisfied()` → `ExitHotel()` fait marcher le monstre vers `SpawnScheduler.spawnPoint` puis le détruit (`WalkToAndDestroy`), aucun chemin identifié vers la réception dans ce fichier. Pistes à vérifier en jeu avant de corriger : le spawn point est-il physiquement proche/derrière le comptoir resto (donnant l'impression visuelle d'un retour) ? Ou `RestaurantReservationSystem` réinscrit-il le visiteur en file quelque part (`HandleSlotIndexChanged`, réservation de slot) sans qu'il soit réellement retiré ? Distinct de Q4 (rééquilibrage des taux/fréquences resto, déjà tracké). |

---

## Cuisine & Stock

| # | Statut | Feature | Notes |
|---|--------|---------|-------|
| C1 | ⬜ | Stock cuisine & réapprovisionnement | Les ingrédients du frigo sont en quantité limitée. Le joueur (ou un employé dédié) doit réapprovisionner. Suivi dans `FridgeStation`. |
| C8 | ⬜ | Gestion de plusieurs cuisines + salariés associés | Demandé par l'utilisateur (précisé : ça inclut la gestion des réceptionnistes du coup — plusieurs `RestaurantReceptionDesk` à répartir, pas seulement les cuisiniers). Aujourd'hui une seule cuisine/un seul `WorkbenchStation` est implicitement supposé — poser plusieurs cuisines et répartir cuisiniers ET réceptionnistes dessus n'est pas conçu (assignation, file d'attente par cuisine, etc. à définir). Voir aussi E6 (affectation salarié en général). |
| C2 | ⬜ | Revenu par plat commandé (pas juste par monstre) | Obsolète corrigé : un revenu est **déjà** généré (`EatingSpot.PayForMeal()` → `EconomyManager.Earn()`, log `[Paiement]`), mais basé uniquement sur `MonsterData.mealRevenue` (flat, par type de monstre) — le plat livré (`HoldableItemData dish`) n'a aucune influence sur le montant. À faire : ajouter un prix par plat/recette et baser le paiement dessus plutôt que (ou en plus de) `mealRevenue`. |
| C3 | ⬜ | Mini-jeux propres à certaines recettes | Certaines recettes (`RecipeData`) déclenchent un mini-jeu dédié pendant leur préparation (au lieu du minutage simple actuel). À concevoir : quelles recettes, quel type de mini-jeu, impact sur la qualité/vitesse de livraison. Possible recoupement avec G6-P5 (système de plats à score/complexité, actuellement en pause) — à clarifier si c'est le même chantier ou deux choses distinctes quand ça démarre. |
| C4 | ⬜ | Icône plat souhaité au-dessus du monstre au resto | Affiche au-dessus du monstre assis (`EatingSpot`) une icône du plat qu'il souhaite — parmi les plats compatibles avec lui uniquement (`HoldableItemData.compatibleMonsters[]` existe déjà, utilisé pour la logique de livraison dans `EatingSpot`, réutilisable pour filtrer). Pattern UI à réutiliser : `WaitingGaugeUI`/`GuestBubble` (déjà affichés au-dessus des monstres ailleurs). |
| C5 | ⬜ | Le monstre peut recommander après son repas | Actuellement `EatRoutine()` termine toujours par `Release()` — la chaise se libère et le monstre repart systématiquement après un seul plat, pas de notion de "recommander". À concevoir : après la fin d'un repas, tirage/choix entre repartir ou rester pour un 2e plat (nouvelle commande), avant de relâcher la place. Lié à C4 (l'icône doit se mettre à jour si le monstre recommande autre chose). |
| C6 | ✅ | Le monstre paie une fois qu'il a mangé, pas en entrant au resto | Vérifié : déjà le comportement actuel (`EatingSpot.PayForMeal()` appelé uniquement en fin de repas). Confirmé en jeu par l'utilisateur — pas de correction nécessaire. |
| C7 | ✅ | Débris de table (après repas) impossibles à ramasser par le joueur | `DebrisInstance.Update()` comparait la distance 3D complète au joueur, alors que le débris de table (`EatingSpot.SpawnMealDebris()`) est en hauteur par rapport au joueur au sol — l'écart vertical seul dépassait `pickupRange` même collé à la table. Même bug déjà rencontré et corrigé côté employé (`CleaningEmployeeAI.HorizontalDistance`), jamais reporté côté joueur. Fix : `DebrisInstance` compare maintenant en distance horizontale (XZ) uniquement, même pattern. |

---

## Environnement & Technique

| # | Statut | Feature | Notes |
|---|--------|---------|-------|
| T1 | ⬜ | Dithering cubes + pièces | Cubes destructibles générés aléatoirement autour de l'hôtel avec dithering. Dithering actif sur toutes les pièces (pas seulement certaines), **y compris tant que le joueur est proche** (précision utilisateur — c'est déjà le principe du système existant sur les murs de pièces, `RoomDither`/`ditherFadeRadius`, juste pas encore étendu à tout). État actuel vérifié dans le code : `ExpansionBlock.ApplyBlockColor()` utilise un URP Lit standard (`_BaseColor`/`_BaseMap`/`_EmissionColor`) — pas encore le shader `MonsterHotel/RoomWallDither` (déjà utilisé pour les murs de pièces, expose `_DitherAlpha`). Le code est déjà prêt à recevoir ce shader sur le matériau du bloc (`_BaseMap` posé via MaterialPropertyBlock, compatible tel quel) — reste à assigner le shader au matériau des blocs et exposer/piloter `_DitherAlpha` (léger, pas le fondu complet des murs). |
| T2 | ✅ | Vérification coop | Audit terminé — voir sous-tâches T2a-T2e ci-dessous. |
| T2a | ✅ | Vol de manette entre joueurs | `PlayerInputBinder.TrySoloDeviceSwitch()` lisait l'état clavier global (positions physiques W/A/S/D = ZQSD en AZERTY) sur chaque joueur, y compris celui à la manette — P1 au clavier faisait basculer P2 (manette) vers le clavier, libérant sa manette que P1 récupérait. Fix : logique désactivée dès qu'un 2e joueur est présent + `neverAutoSwitchControlSchemes = true` sur P2 (P1 l'avait déjà) dans `LocalJoinManager`/`LocalJoinOnHold`. |
| T2b | ✅ | Fermeture UI à la manette (Cancel) | `FridgeUI`/`RoomShopPanel` : déjà bon. `EmployeeHiringPanel` : entièrement retravaillé — navigation manette complète (Navigate/Submit/Cancel/tabs), scopé au `PlayerInput` passé à `Show(pi)`. `RoomManagementPanel` : avait déjà Navigate/Submit/tracking joueur, ajout de Cancel manquant. |
| T2c | ✅ | UI illisible pour P2 (débordement horizontal) | Corrigé par l'utilisateur directement dans l'Inspector (config `content`/`LayoutGroup`). |
| T2d | ✅ | Scoping par joueur des panneaux UI | `EmployeeHiringPanel.Show(PlayerInput)` — le panel connaît maintenant le joueur qui l'a ouvert. `EmployeeBoardTrigger` passe le `PlayerInput` du joueur déclencheur. |
| T2e | ✅ | Audit split-screen caméras/viewports | Chaîne vérifiée : `PlayerRegistrar` → `GameManager.RegisterPlayer` → `SplitScreenManager.HandlePlayerJoined` — correcte. Le split-screen (`ProjectDawn.SplitScreen`) compose 2 caméras en post-traitement dans un seul écran final (pas de vrais viewports séparés) — un seul Canvas plein écran avec positionnement manuel par joueur (bas-gauche/bas-droite) est donc la bonne approche, pas un bug. Trouvé au passage : `SplitScreenManager.cameraHeight` orphelin (jamais lu), laissé tel quel (cohérent avec B4). |
| T3 | ⬜ | Meubles ne carvent pas le NavMesh | Seul le placement de **pièces** rebake le NavMesh (`RoomPlacer.RebakeNavMeshAsync` + `NavMeshObstacle` sur `RoomPlaceholder`). `FurniturePlacer` n'ajoute aucun `NavMeshObstacle` — un meuble seul posé sur un chemin de monstre (ex : ligne d'attente cuisine, cf. G6-B4) n'est pas contourné par le pathfinding. Trouvé en corrigeant G6-B4. |
| T5 | 🔧 | Points de position dédiés pour l'interaction meuble | Implémenté pour les comptoirs de réception (hôtel + resto) suite au bug réceptionniste bloqué : `ReceptionDesk.StandPoint`/`RestaurantReceptionDesk.StandPoint` cherchent un enfant nommé `"StandPoint"`, sinon retombent sur le pivot (non-cassant). 🔍 **À faire dans l'éditeur : ajouter l'enfant `StandPoint` sur les prefabs des 2 comptoirs**, sinon aucun changement de comportement. Reste à généraliser le même pattern à `ChairSlot`/tables et aux autres meubles interactifs si le même souci s'y présente. |
| T6 | ✅ | Supprimer (ou déplacer/améliorer) un meuble ne passe pas en vue construction | Nouveau `TopDownController.WantsCameraZoom` (`IsBuilding \|\| FurniturePicker.IsPicking`), volontairement séparé d'`IsBuilding` : celui-ci verrouille aussi le déplacement du joueur, alors que `FurniturePicker` a besoin qu'il puisse marcher jusqu'au meuble à sélectionner. `PlayerCamera`/`SplitScreenManager` utilisent maintenant `WantsCameraZoom` au lieu d'`IsBuilding` pour le dézoom/la logique split-écran. S'applique aux 3 modes (Move/Upgrade/Remove), pas seulement Remove. |
| T7 | 🔍 | Ghost meuble en rouge si superposition avec un autre meuble de la pièce | Signalé par l'utilisateur ("à vérifier"), reconfirmé depuis. Déjà implémenté dans le code : `FurniturePlacer.UpdateGhostTransform()` teste le chevauchement avec `_targetRoom.PlacedFurniture` et bascule `_currentValid = false` → `ghostInvalidMaterial` (rouge) en cas d'intersection. Si ça ne se voit pas en jeu, vérifier en premier que `ghostInvalidMaterial` est bien assigné sur `FurniturePlacer` dans l'Inspector (le champ existe mais rien ne force son remplissage). **Portée à clarifier** : le code actuel ne couvre que le chevauchement meuble/meuble — si le besoin est plus large ("ne peut pas être posé" pour d'autres raisons, ex : hors pièce), ce serait une extension distincte à concevoir. |
| T11 | ⬜ | Déplacement de meuble — retour à la position de départ si validation invalide | Demandé par l'utilisateur, confirmé absent actuellement : en mode déplacement (`FurniturePicker`/`FurniturePlacer`, voir T6), le meuble ne revient pas à sa position d'origine si la validation échoue/est annulée — comportement souhaité, pas encore implémenté. |
| T8 | ⬜ | Limitation du nombre de meubles par pièce/cuisine | Demandé par l'utilisateur. Pas de plafond actuellement — `RoomInstance.PlacedFurniture` accepte sans limite tant qu'il y a la place physique. À concevoir : cap fixe, ou basé sur la taille/le type de pièce. |
| T9 | ⬜ | Limitation du nombre de pièces selon la taille de l'hôtel (agrandir débloque plus de pièces) | Demandé par l'utilisateur. Pas de lien actuel entre excavation (`ExpansionBlock`/blocs Terre-Roche) et un plafond de chambres — n'importe quelle taille d'hôtel permet actuellement autant de pièces que l'espace le permet physiquement, sans cap dédié. À concevoir : compteur de pièces vs surface excavée. |
| T4 | ⬜ | Rationaliser les touches manette | De nombreux systèmes joueur partagent tous la même action "Interact" (`EatingSpot`, `CleaningInteractor`, `MonsterFightBreaker`, `EmployeeInteractor`, `ReceptionInteractor`, `RestaurantReceptionInteractor`...) — à passer en revue pour vérifier qu'il n'y a pas de conflit/ambiguïté quand plusieurs contextes se chevauchent, et voir si `InputConsumer` (singleton déjà présent pour "consommer" l'appui Interact pour un joueur/frame donné, mais pas utilisé par la plupart de ces interactors) devrait être adopté plus largement. Revoir aussi l'ensemble du mapping manette (`Assets/InputSystem_Actions.inputactions`) pour cohérence générale. |
| T10 | ⬜ | Système de dégradation sur les décorations, perte de Confort associée | Demandé par l'utilisateur suite aux questions sur le calcul du score. Actuellement `DecorationInstance` (`Assets/Scripts/Systems/Hotel/DecorationInstance.cs`) n'a aucun état d'usure — son `comfortBonus`/`renownBonus` (`DecorationData`) compte en permanence tant qu'elle reste posée (`Register`/`Unregister` sur pose/destruction uniquement, voir `HotelStatsManager`), contrairement aux meubles de chambre qui eux peuvent devenir sales/abîmés (`FurnitureInstance.IsDirty`/`IsDamaged`). À concevoir : un état de dégradation similaire sur les décorations (usure dans le temps ? liée au passage de monstres ? nécessitant un entretien du joueur comme le nettoyage des chambres ?), qui réduirait tout ou partie de `comfortBonus` tant que non réparée/nettoyée — impact direct sur `HotelStatsManager.TotalComfort`, donc sur le score global (`globalComfortWeight`, voir U2) et sur la satisfaction initiale des monstres (`SatisfactionConfig.comfortToSatisfactionBonus`). |

---

## 🔄 Refontes majeures

| # | Statut | Feature | Notes |
|---|--------|---------|-------|
| RW1 | ✅ | Écran scindé statique + fusion caméra si joueurs proches | `SplitScreenManager` n'utilise plus le package `ProjectDawn.SplitScreen` — deux caméras indépendantes avec `Camera.rect` fixe (P1 gauche, P2 droite ; plein écran en solo). `PlayerCamera.cs` consolide `CameraShaker`/`BuildModeCameraZoom`/`BuildCursorTargetProvider` (tous supprimés). `Camera.main` remplacé par la caméra propre à chaque joueur là où c'était utilisé pour du calcul relatif au joueur (curseur de placement, occlusion dithering). **2 bugs trouvés au playtest et corrigés** : (1) l'ancienne Main Camera, même neutralisée par culling/clear, se dessinait par-dessus `cam0`/`cam1` et effaçait tout (depth plus élevée) — désactivée entièrement, tag `MainCamera` repris par `cam0`. (2) `LocalJoinOnHold` ne positionnait jamais les joueurs qui rejoignent (P2 spawnait à l'origine du monde) — corrigé, réutilise le spawn point de `AutoSpawnP1`. **Ajout demandé après coup** : fusion caméra si les deux joueurs sont proches (`SplitScreenManager.mergeDistance`/`mergeHysteresis`, défauts 15/3, à ajuster) — une seule caméra plein écran cadre les deux au lieu de rester scindé en permanence ; toujours une coupure verticale nette quand scindé, jamais orientée. Désactivée pendant la construction (RW2) pour ne pas interférer avec le dézoom/curseur. Confirmé fonctionnel en jeu. Nettoyage optionnel différé (pas bloquant) : retirer `SplitScreenRendererFeature` de `PC_Renderer.asset` et la dépendance `com.projectdawn.splitscreen` du manifest si plus rien ne l'utilise. |
| RW2 | ✅ | Construction en vue aérienne avec grille (remplace le déplacement joueur) | Implémenté 2026-07-27 : curseur de placement découplé du joueur (`RoomPlacer`/`FurniturePlacer`/`DecorationPlacer`, stick droit/flèches), dézoom caméra, cadrage sur le curseur, grille visuelle, verrouillage du déplacement pendant la construction. Repasse en vrai coop à 2 confirmée par l'utilisateur (dézoom/grille isolés au bon joueur, mode scindé forcé pendant la construction sans fusion caméra intempestive) — dernier point ouvert (revérification post-RW1) désormais validé. |
| RW3 | ⬜ | Rationaliser toute l'UI du jeu sur le même système que `DayEndPanel` | Demandé par l'utilisateur après la migration de `DayEndPanel` vers UI Toolkit (`UIDocument`/`VisualElement`, structure statique en UXML écrit à la main + contenu dynamique généré par code, styles pilotés par des champs Inspector, effets Feel/MMFeedbacks ciblables par nom). Tout le reste de l'UI du jeu (`RoomManagementPanel`, `EmployeeHiringPanel`, `FridgeUI`, `RoomShopPanel`, `GuestBubbleSlot`, `QualityBarUI`, `TimeHUD`, `WaitingGaugeUI`, `GoldHUD`, `GuestQueueUI`, `HiredSlot`, `CandidateSlot`, etc.) est actuellement en uGUI classique (Canvas/RectTransform/TextMeshProUGUI), construit à la main dans l'éditeur. Chantier de grande ampleur (refonte de chaque panel un par un, pas un simple renommage) — à cadrer précisément (ordre de priorité entre panels, ce qui reste en uGUI vs bascule en UI Toolkit) avant de commencer plutôt que de tout attaquer d'un coup. Voir U2 (CHANGELOG 2026-08-19/20) pour le détail des pièges déjà rencontrés et résolus sur `DayEndPanel` (ordre Awake/OnEnable, UXML vs génération par code, Timescale Feel pendant la pause) — directement réutilisables pour éviter de les re-découvrir sur chaque nouveau panel. |

---

## Prérequis Unity à configurer dans la scène

| Composant | GameObject | Remarques |
|-----------|-----------|-----------|
| `ReceptionDesk` | Comptoir de réception | Marker component, pas de logique |
| `WorkbenchStation` | Plan de travail cuisine | Requis par CookEmployeeAI ; `recipes[]` à remplir |
| `FridgeStation` | Frigo | `ingredients[]` à remplir avec les `HoldableItemData` |
| `EatingSpot` | Chaque chaise/siège | `All` géré automatiquement par OnEnable/OnDisable |
| `EmployeeBoardTrigger` | Panneau d'annonces | Ouvre `EmployeeHiringPanel` au press Interact |
| `EmployeeInteractionWheel` | Canvas WorldSpace enfant du prefab employé | Assigner `wheelRoot`, `fireButton`, `breakButton`, `workButton`, `stateLabel` |
| `HotelConfig` asset | `Resources/HotelConfig` | Remplir `recipes[]`, `monsters[]`, `rooms[]`, `blocks[]`, `needTypes[]` |

---

## 🐛 Bugs trouvés au playtest (2026-07-15)

| # | Statut | Bug | Notes |
|---|--------|-----|-------|
| B1 | ✅ | Vampire/Loup-Garou ne spawnent jamais | `SpawnScheduler.TrySpawn()` : la fenêtre d'ouverture (`spawnOpenHour`-`spawnCloseHour`) et la fenêtre `NightOnly` étaient mutuellement exclusives. Fix : la fenêtre d'ouverture générale ne gate désormais que les monstres `SpawnTime.Any` — les monstres avec une préférence jour/nuit explicite (Day/NightOnly) s'appuient uniquement sur cette préférence, qui prime. |
| B2 | ⬜ | Matching recette/ingrédient par monstre inactif (pas cassé) | `HotelConfig.recipes` est vide (`recipes: []`). `CookEmployeeAI.FindRecipeForMonster()` retourne alors `null`, et `EatingSpot.CompleteDelivery()` bascule sur la branche fallback (remplit le besoin le plus urgent directement) — le service se termine correctement, satisfaction appliquée. Le système fonctionne donc de bout en bout, mais le vrai matching recette↔ingrédient↔type de monstre ne se déclenche jamais faute de données. À remplir `HotelConfig.recipes[]` pour activer cette logique. |
| B3 | ⬜ | Effondrement de satisfaction quasi instantané dès besoin critique | `NeedType_Hunger.satisfactionDecayPerSecond = 10` — une fois un besoin critique, perte de 10 satisfaction/seconde. Avec ~80 de satisfaction de départ et `leaveThreshold = 10`, ça donne un effondrement total en ~7 secondes. Combiné à B2 (cuisine cassée), explique très probablement les zombies qui repartent avant la fin de leur séjour. |
| B4 | ⬜ | `HotelConfig.blockHeight` orphelin | Champ déclaré mais **lu nulle part** dans le code — le modifier dans l'Inspector n'a aucun effet en jeu. Décision : laissé tel quel pour l'instant, pas câblé. |
| B5 | ✅ | `HotelConfig.monsters`/`rooms` câblés en source unique | `SpawnScheduler.monsterPool` et `ShopCounter.catalog` sont désormais écrasés par `HotelConfig.monsters`/`rooms` au démarrage (s'ils sont non-vides). **⚠️ `HotelConfig.rooms` ne contient qu'1 seule chambre actuellement — à compléter dans l'Inspector avec toutes les chambres (Zombie/Vampire/Loup-Garou) avant relance, sinon le magasin perd des options.** `needTypes` non câblé (pas de catalogue local équivalent — chaque monstre a ses besoins propres via `MonsterData.needs[]`). `blocks` non câblé (assignation par anneau ordonnée dans `ExpansionBlockSpawner.ringBlocks[]`, forcer une liste non-ordonnée casserait la progression Terre/Roche). |
| B6 | ⬜ | Valeurs `HotelConfig.asset` modifiées manuellement hors suivi | `dayDuration: 500` (au lieu de 300, cause probable du "temps trop long"), `spawnOpenHour: 8`, `roamMinWait`/`roamMaxWait: 1` (quasi-instantané, au lieu de 15/40), `playerMoveSpeed`/`playerSprintSpeed` doublés, `employeeMaxOverride: 6`, `ditherHeightOffset`/`ditherFadeRadius` très éloignés des défauts. À trier : tests volontaires vs oublis, avant l'équilibrage. |
| B7 | ✅ | `ReservationSystem.ProcessQueue()` code mort | Supprimé (méthode + les 2 appels dans `RoomInstance.AddFurniture()`/`SetState()`) — confirmé sans effet fonctionnel avant suppression. |
| B8 | ✅ | Employé de nettoyage ne se déplace plus mais exécute quand même l'action | Cause trouvée : débris de repas sur table = hauteur non nulle, mais le mover verrouille le Y du nettoyeur au sol. Le test d'arrivée comparait la distance 3D complète (jamais franchissable à cause de l'écart de hauteur) → timeout systématique → ramassage via fallback. Fix : distance horizontale (XZ) uniquement. Ne concernait que les débris sur table, pas ceux au sol. Voir CHANGELOG 2026-07-20 (suite). |
| B9 | ⬜ | Un monstre n'ayant pas mangé retourne à la réception | Signalé par l'utilisateur, pas encore investigué. À vérifier en premier : `MonsterNeedSeeker.GiveUpMeal()` (branche client chambre) le fait retourner en chambre, pas à la réception — si le comportement observé est bien "retourne à la réception", la cause est probablement ailleurs (peut-être confondu avec la file resto elle-même, ou un monstre qui redemande un check-in). À reproduire pour localiser le point exact. |
| B10 | ⬜ | Déplacement de meuble : revient à la position de départ à la validation | Signalé par l'utilisateur ("à vérifier"). `FurniturePlacer.TryPlace()` (branche `_movingFurniture != null`, ~ligne 401) repositionne bien sur `_ghostWorldPos` à la confirmation — pas de bug évident à la lecture. Pistes à vérifier en jeu : le meuble reste-t-il invalide (`_currentValid = false`, la confirmation est alors silencieusement ignorée, ligne 398) au moment où on pense valider ? ou le ghost affiche-t-il une position qui n'est pas celle réellement appliquée ? |
| B11 | ⬜ | Après un certain temps de jeu (beaucoup de monstres dans l'hôtel), plus moyen de cuisiner un ingrédient | Signalé par l'utilisateur, pas encore reproduit. Piste probable en lisant `WorkbenchStation.cs` : sa state machine (`Idle → WaitingQualityCapture → ReadyToPickup → Idle`) est **entièrement pilotée par le joueur** (`Update()` ne réagit qu'à `PlayerItemHolder.All`, pas aux employés — `CookEmployeeAI` référence juste `_bench` pour se positionner, ne pilote pas cet état). `AutoCaptureAfter()` fait avancer automatiquement `WaitingQualityCapture → ReadyToPickup` si le temps s'écoule, **mais rien ne fait jamais revenir `ReadyToPickup → Idle` sauf un joueur qui vient récupérer le plat** (`GiveResultToPlayer`). Un seul plat oublié/jamais récupéré (joueur distrait, parti faire autre chose) bloque tout le plan de travail indéfiniment — `TryStartCrafting` ne fait rien tant que `_state != Idle`. Plausible que ça arrive plus souvent avec beaucoup de monstres (plus de cycles de cuisine = plus de chances qu'un soit abandonné). Pas de mécanisme de récupération/timeout sur `ReadyToPickup` actuellement. À confirmer en jeu avant de corriger. |

---

## 🧹 Refacto identifiée (sans casser l'existant)

| # | Statut | Piste | Notes |
|---|--------|-------|-------|
| R1 | ✅ | Duplication entre les 3 IA employé | Extrait dans `EmployeeTaskAI` (classe de base abstraite) : cycle Working/GoingToWork, `_busy`, `EndTask()`, `RatingSpeedMultiplier()`. Sous-classes réduites à `GoToPost()`/`TryStartTask()`/`ReleaseReservations()`. Bug latent corrigé au passage : `CookEmployeeAI`/`CleaningEmployeeAI` ne stoppaient pas leur coroutine en cas d'interruption externe (contrairement à `ReceptionEmployeeAI`), maintenant uniforme. |
| R2 | ⬜ | Nettoyer les champs orphelins de `HotelConfig` | `blockHeight` (jamais lu), `blocks`/`monsters`/`rooms`/`needTypes` (catalogues jamais lus par le code, juste des raccourcis Inspector). Voir B4/B5. Soit les câbler pour de vrai, soit les retirer/documenter clairement comme purement décoratifs pour éviter la confusion. **+ `nightStayDuration`** (ajouté puis reverté le 2026-07-16 — la durée de séjour reste liée à `dayDuration`, comportement voulu). Laissé déclaré pour éviter de redéclencher l'erreur de build "script layout incompatible" — à retirer proprement lors d'une prochaine passe de compilation (fermer/rouvrir Unity avant et après). |
| R3 | ✅ | Éclater `HotelConfig` en plusieurs fichiers de config par catégorie | Éclaté en 15 ScriptableObjects par domaine (`EconomyConfig`, `SatisfactionConfig`, `ReceptionConfig`, `KitchenConfig`, `DayNightConfig`, `SpawnConfig`, `PlayerConfig`, `PlacementConfig`, `CameraConfig`, `DitherConfig`, `BlockConfig`, `RoamConfig`, `EmployeeConfig`, `DebugConfig`, `HotelCatalog`), chacun auto-chargé depuis `Resources/Config/` (même pattern singleton qu'avant, pas de câblage manuel). `HotelConfig` devient une façade statique (`HotelConfig.Economy`, `HotelConfig.Satisfaction`, ...) — point d'accès central conservé, 25 call sites mis à jour, valeurs live migrées à l'identique (y compris les valeurs B6 non triées). |

---

## Équilibrage

| # | Statut | Feature | Notes |
|---|--------|---------|-------|
| Q1 | 🔧 | Équilibrage global | Économie, séjours, coûts installations et blocs faits (voir CHANGELOG 2026-07-10). Playtest du 2026-07-15 a révélé des bugs bloquants (voir section ci-dessus) qui faussent tout ressenti d'équilibrage tant qu'ils ne sont pas corrigés. Restent aussi : réputation/légendaires (pas encore implémenté), à valider sur une session de jeu complète une fois B1-B6 traités. |
| Q3 | ⬜ | Équilibrage coûts/gains | Revoir l'ensemble des coûts (chambres, meubles, blocs, formations...) et des gains (revenus par nuit, repas, pourboires) pour un ressenti économique cohérent sur une session complète. |
| Q2 | ⬜ | Revoir le lien entre `dayDuration` et `nightStayDuration` | État actuel vérifié dans le code : `HotelConfig.nightStayDuration` (60s par défaut) est déclaré avec un tooltip explicite ("découplée de dayDuration") mais **n'est lu nulle part** — `ReservationSystem.TryAssignRoom()` calcule directement `actualDuration = nights * dayDuration` (`ReservationSystem.cs:305-306`), donc une "nuit" de séjour dure en réalité un cycle jour/nuit complet (`TimeManager.dayDuration`, 300s par défaut), pas la durée dédiée `nightStayDuration`. Un revert précédent (CHANGELOG 2026-07-16) avait volontairement gardé ce lien direct, mais le champ orphelin reste présent et prête à confusion (voir aussi R2). À trancher : soit brancher `nightStayDuration` pour de vrai (séjour découplé du cycle jour/nuit), soit retirer le champ pour éviter l'ambiguïté — décision d'équilibrage à prendre par l'utilisateur, pas à deviner. |
| Q4 | ⬜ | Taux de client resto à rééquilibrer | Demandé par l'utilisateur. Concerne le nouveau flux visiteur repas (G9) : `MonsterData.mealVisitorSpawnInterval` par monstre + `SpawnConfig.defaultMealVisitorSpawnInterval` (30s de départ) — à ajuster une fois testé en jeu, le rythme actuel n'est qu'une valeur de départ jamais calibrée. |
| Q5 | ⬜ | Équilibrage salarié | Demandé par l'utilisateur. Revoir `EmployeeConfig` (salaire, frais d'embauche, coûts formation/licenciement à venir — voir items 10/11/13/14 du Système Employés) pour un ressenti cohérent sur une session complète. |

---

## Animation

| # | Statut | Feature | Notes |
|---|--------|---------|-------|
| A1 | ✅ | Blend directionnel marche joueur (skin Tall) | `TopDownController.cs` calcule `MoveX`/`MoveZ` (direction de déplacement en espace local) + damping via `Animator.SetFloat(..., dampTime, ...)`, snap à la cible une fois l'écart négligeable (`SetAnimFloat()`). Rotation `RotateTowards` (vraie vitesse angulaire, `rotationSpeed = 480°/s` de départ, à ajuster — remplace un `Slerp` mal calibré qui snappait la rotation en un seul tick). `Tall.controller` : état `Walk` branché sur un `BlendTree` Freeform Directional 2D (8 clips `EverydayMotionPack/Motion/02_Move/@male_move_walk_*`). Décision utilisateur : pas de vrai strafe, juste une rotation moins abrupte — `rotationSpeed` à retrouver soi-même selon le feeling voulu (trop bas = effet "strafe" visible pendant les virages serrés). Testé et validé par l'utilisateur. |
| A1b | 🔍 | Réglages fins restants (joueur) | `moveBlendDampTime` (TopDownController) et `Transition Duration`/seuil `Speed` de la transition Walk→Idle (`Tall.controller`, dans l'Animator) — tous ajustables dans l'éditeur, pas de code. Contrôlent le délai perçu avant de repasser en Idle après un arrêt. |
| A2 | 🔧 | P2 avec un personnage différent de P1 (au lieu d'un clone) + skin Small/Monster | Demandé par l'utilisateur. `Player.prefab` contenait déjà les deux modèles (Tall actif, Small désactivé) — nouveau `PlayerSkinSelector.cs` (root du prefab) active le bon enfant selon `PlayerInput.playerIndex`, appelé explicitement juste après chaque `PlayerInput.Instantiate()` (`AutoSpawnP1.cs`, `LocalJoinOnHold.cs` — pas `LocalJoinManager.cs`, du code mort absent de la scène). `TopDownController.RefreshAnimator()` (nouveau) recale la référence Animator mise en cache par `Awake()`, sinon elle reste bloquée sur Tall (piège d'ordre : `GetComponentInChildren` ignore les enfants inactifs, et `Awake()` tourne pendant `Instantiate()` avant que le sélecteur n'ait pu changer l'enfant actif). Petit rig Generic (`animationType: 2`) réimporté en Humanoid (`animationType: 3`, comme Tall et les 3 monstres — en fait la norme du projet, Small était l'exception) pour débloquer de vraies animations (`EverydayMotionPack`) au lieu du stub Idle/Walk à 2 clips. **🔍 Setup Unity requis, dans cet ordre : (1) vérifier Configure Avatar sur le FBX Small (Rig → Humanoid) — pas d'os rouge/manquant ; (2) si OK, assigner `Tall.controller` (ou un `Monster_AC.controller` reconstruit pareil) sur l'Animator de l'enfant Small ; si le mapping échoue, revenir en Generic et sourcer un pack d'anims dédié (même solution que `Zombie Idle.fbx` pour Zombie) ; (3) ajouter `PlayerSkinSelector` sur le root de `Player.prefab`, glisser Tall en index 0 / Small en index 1 ; (4) vérifier/ajuster le `CapsuleCollider` du root si les proportions de Small diffèrent trop de Tall.** Sans ce setup, comportement inchangé (P1/P2 identiques comme avant). |
| A3 | ✅ | Lissage Idle↔Walk + rotation monstres (Zombie/Vampire/Werewolf) | `MonsterMover.cs` : `Speed` (0/1) n'est plus poussé instantanément mais approché en continu via `Animator.SetFloat(..., dampTime, ...)` (nouveau tunable `speedBlendDampTime = 0.15f`, à ajuster). Rotation dans `DirectMove()` passée de `Slerp` (facteur fixe non exposé) à `RotateTowards` (vraie vitesse angulaire, nouveau champ `rotationSpeed = 360°/s`, à ajuster). Pas de Blend Tree directionnel ici (délibéré) : les monstres se réorientent déjà vers leur direction de déplacement via `DirectMove()`, un blend 8 directions tomberait dans le même piège que celui résolu pour le joueur. |
| A4 | ⬜ | Animations d'assise monstres (repas) | Idée utilisateur : faire s'asseoir les monstres quand ils mangent, via clips `@male_act_sit_on_chair`/`@female_act_sit_on_chair` (`EverydayMotionPack/Motion/04_Interaction/`). **À vérifier avant de commencer : rig des monstres (Humanoid vs Generic) — leurs clips Idle/Walk actuels référencent des guids distincts par monstre (pas le pack partagé), à confirmer si retargetable.** |
| A5 | ⬜ | Animations liées aux meubles/objets (au-delà de l'assise) | Idée utilisateur, plus large que A4/S3 : des animations spécifiques selon le meuble utilisé, pas seulement s'asseoir — ex: le cuisinier qui gesticule/coupe au plan de travail, interaction avec le frigo, etc. Vérifié : aucune IA employé (`CookEmployeeAI`, `CleaningEmployeeAI`...) ne déclenche quoi que ce soit sur l'Animator aujourd'hui, juste l'Idle/Walk générique de `MonsterMover` — le besoin est réel, pas encore du tout couvert. À concevoir : un mécanisme générique (par ex. un champ sur `FurnitureData`/`WorkbenchStation` référençant un clip + un paramètre Animator à déclencher) plutôt que du code en dur par meuble, pour rester extensible. Recoupement à clarifier avec A4/S3 (assise) et S1b (variété d'anim conversation) le moment venu — probablement la même famille de chantier "animations contextuelles" vue sous plusieurs angles. |

---

## Interactions sociales monstres

Chantier scindé en 4 sous-parties indépendantes (demande utilisateur). Plan détaillé de la 1ère
partie : `C:\Users\Virgile\.claude\plans\elegant-prancing-pudding.md`.

| # | Statut | Feature | Notes |
|---|--------|---------|-------|
| S1 | ✅ | Conversations entre monstres compatibles | `MonsterSocialBehavior.cs` (nouveau) + `compatibleSocialTypes[]` sur `MonsterData`. Recherche opportuniste (pas de pathfinding longue distance), verrouillage mutuel synchrone, anim `IsTalking` (clip `@male_talk_stand`) sur les 3 `.controller`. Impact : bonus de satisfaction (`satisfactionBonus`) en fin normale + cooldown anti-boucle (`cooldownAfterChat`) après chaque conversation. 🔍 **À faire dans l'éditeur : remplir `compatibleSocialTypes` sur au moins 2 `MonsterData`** (vide par défaut, sinon aucune conversation ne se déclenchera jamais), puis playtest (2 monstres compatibles en balade doivent se rejoindre et discuter ; interruption propre si besoin urgent/checkout pendant la conversation — vérifier qu'aucun des deux ne reste bloqué `IsBusy` ; vérifier que le cooldown empêche bien un ré-enchaînement immédiat). |
| S1b | ⬜ | Variété d'animation pendant la conversation | Actuellement les deux monstres jouent toujours le même clip (`@male_talk_stand`) sans distinction de rôle. Idée évoquée : alterner `talk_stand`/`nod_stand` (parle/écoute), ou `greet` au moment où ils se rejoignent avant la boucle. Pas encore implémenté — à faire si souhaité. |
| S2 | ✅ | Bagarre entre monstres incompatibles + séparation par le joueur | `MonsterFightBehavior.cs` (composant frère de `MonsterSocialBehavior`) + `incompatibleTypes[]` sur `MonsterData` + `MonsterFightBreaker.cs` (interaction joueur, pattern `CleaningInteractor`). Timeout → séparation auto avec pénalité sévère ; joueur intervient à temps (`Interact` à proximité) → pénalité faible. VFX `CFXR2 Cartoon Fight (Loop)` + `Fight Burst Prefabs` pondérés/décalés par entrée. Anim placeholder (`@male_emotion_angry`) sur les 3 `.controller` — à remplacer par une vraie anim de bagarre quand trouvée (seul point encore ouvert). Testé et confirmé en jeu par l'utilisateur (déclenchement, VFX, séparation joueur, timeout, interruptions prioritaires, cohabitation avec S1, cooldown). |
| S3 | ⬜ | Animation d'assise repas + lounge | Repas : `EatingSpot` existe déjà (réservation, `ChairSlot`) mais aucune animation d'assise visuelle actuellement (position figée). Lounge : pas d'équivalent `LoungeSpot` aujourd'hui — `FacilityRoomInstance` fait juste stationner le monstre au centre de la pièce, sans siège individuel. Clips dispo : `@male_act_sit_on_chair`/`@female_act_sit_on_chair` (`EverydayMotionPack/Motion/05_Act/`). |
| S4 | ⬜ | Sommeil dans les lits | Aucune notion de lit occupable aujourd'hui (`RoomInstance.Assign()` est purement administratif, pas de `BedSlot`). `HotelConfig.nightStayDuration` existe mais est mort dans le code (revert documenté, voir CHANGELOG). À concevoir : `BedSlot` (même pattern que `ChairSlot`), routine d'entrée/sortie de chambre la nuit, anim allongé par type de monstre (clips `@male_act_lie_face_down`/`@male_idle_on_floor` à adapter). Lié à G3 (comportement nuit des monstres, non commencé). |

---

## Backlog (idées futures)

- Spécialisation des employés (un seul rôle par employé ou polyvalence upgradable)
- Événements aléatoires (employé malade, surprise inspection, etc.)
- UI de statut employé en jeu (jauge fatigue/bien-être visible)
- Plusieurs cuisiniers en parallèle (gestion des slots EatingSpot sans collision)
- Bug latent `MonsterMover` : la surcharge `MoveTo(entryPoint, finalTarget)` (mouvement deux phases) n'invoque jamais l'event `OnArrived`, contrairement à `MoveTo(target)` (une phase). Repéré en corrigeant le bug des employés bloqués en pause — pas encore d'impact fonctionnel connu, mais à surveiller si un futur système dépend de `OnArrived` avec un déplacement deux phases.
- Système de réputation de l'hôtel lié à la qualité des employés (distinct de G1)

---

## Récap fin de journée — éléments ciblables par Feel (référence)

Pour ajouter/modifier des effets Feel (`MMF Player` + feedbacks UI Toolkit) sur `DayEndPanel`.
Toujours : `Target Document` = `DayEndCanvas`, et **`Timescale Mode` = `Unscaled`** (ou `Force
Timescale Mode` coché au niveau du Player) — le jeu est en pause pendant que ce panel est affiché,
un feedback en `Scaled` se fige dès la pause et ne joue jamais.

**Éléments fixes** (existent une fois pour toutes, définis dans `DayEndPanel.uxml`) — `Query Mode = Name` :
- `Panel` — le cadre principal du récap
- `DayEndRoot` — tout l'écran, voile sombre inclus
- `TabBar` — la colonne des 3 onglets
- `Tab_0` / `Tab_1` / `Tab_2` — chaque bouton d'onglet (0=Général, 1=Journalier, 2=Monstres)
- `TabLabel_0` / `TabLabel_1` / `TabLabel_2` — le texte de chaque bouton d'onglet (à cibler pour un
  effet sur la police/texte du bouton, PAS `Tab_X` qui est le conteneur)
- `ContentArea` — la zone de contenu à droite (tous onglets confondus)
- `Content_0` / `Content_1` / `Content_2` — le contenu (ScrollView) de chaque onglet
- `DismissPrompt` — le texte "[ Submit ] Continuer"

**Lignes de stats** (détruites et recréées CHAQUE JOUR par `RebuildLines()` — voir piège timing
ci-dessus, `Initialization()` doit être rappelé à chaque affichage, déjà fait dans le code) :
- Nom individuel : `{conteneur}_Line_{index}`, ex. `Content_1_Line_0` = 1ère ligne de l'onglet
  Journalier ("Jour X terminé"). Index stable d'un jour à l'autre (même position = même nom), mais
  le contenu texte change.
- Classe commune à toutes les lignes, tous onglets confondus : `stat-line` (`Query Mode = Class`) —
  pour animer toutes les lignes d'un coup plutôt qu'une par une.

**Déclenchement dans le code** (`DayEndPanel.cs`) :
- `Show Feedback` (champ) : joué une fois à l'ouverture du récap (`Show()`).
- `Tab Bounce Feedbacks` (tableau, **toujours exactement 3 éléments**, index 0/1/2 = un onglet
  chacun) : joué quand l'onglet correspondant devient actif (ouverture ET clic) — ne jamais ajouter
  un 4e élément, il ne serait jamais lu par le code.
- Pour un effet à un autre moment (fermeture, etc.) : pas de mécanisme existant, il faut demander un
  nouveau câblage dans le script.

**Ordre des lignes actuel** (pour retrouver `Line_N`) :
- `Content_0` (Général) : 0=Jour actuel, 1=Score global, 2=Étoiles, 3=Or total, 4=Satisfaction
  moyenne globale, 5=Renommée totale, 6=Confort total, 7=Arrivées au total, 8=Clients servis au
  total, 9=Gain resto total, 10=Monstres présents.
- `Content_1` (Journalier) : 0=Jour terminé, 1=Score, 2=Étoiles, 3=Or, 4=Satisfaction moyenne,
  5=Arrivées, 6=Clients servis, 7=Gain resto, 8=Renommée gagnée.
- `Content_2` (Monstres) : un **bloc** de plusieurs lignes par valeur de l'enum `MonsterType` (titre +
  une stat par ligne : Présents, Satisfaction moyenne, Départs aujourd'hui, Renommée globale,
  Renommée gagnée/perdue aujourd'hui, Gain resto aujourd'hui/total), séparés par une ligne vide —
  index `Line_N` variable selon le nombre de types et leurs données, pas de correspondance fixe
  utilisable comme pour Content_0/Content_1.
